<project basedir="." default="build" name="synthscape">
  <description>synthscape</description>
  <property file="build.properties" />
  
  <target name="init">
    <mkdir dir="${build.dir}"/>
  </target>
  
  <target name="clean">
    <delete dir="${build.dir}"/>
  </target>
  
  <target name="build" depends="init">
    <javac 
       includeantruntime="false"
       destdir="${build.dir}" 
       deprecation="${javac.deprecation}" 
       debug="${javac.debug}" 
       debuglevel="${javac.debuglevel}" 
       optimize="${javac.optimize}" >
      
      <classpath>
	<fileset dir="${lib.dir}">
	  <include name="**/*.jar"/> 
	</fileset>
      </classpath>
      <src>
	<pathelement location="${source.main.dir}" />
      </src>
    </javac>
  </target>
  
</project>
